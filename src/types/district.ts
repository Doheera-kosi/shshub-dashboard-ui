export interface District {
  id: string;
  name: string;
  createdAt: string;
  lastUpdated: string;
  createdBy: string | null;
  updatedBy: string | null;
}

export interface District {
  id: string;
  name: string;
  region: string;
}